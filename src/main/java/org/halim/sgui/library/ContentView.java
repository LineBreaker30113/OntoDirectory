package org.halim.sgui.library;

import org.halim.Listeners.TagChangeListener;

import javax.swing.*;

/** Views that display the contents of a specifically selected tag (Files, Notes) */
public abstract class ContentView extends JPanel implements TagChangeListener {
// These need to know when the user clicks a specific tag in the StructureView
}
